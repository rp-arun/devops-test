  apiVersion: argoproj.io/v1alpha1
  kind: ApplicationSet
  metadata:
    name: cmk-core-appset
    namespace: argocd
  spec:
    generators:
    - git:
        repoURL: https://github.com/credmark-devops/gitops.git
        revision: HEAD
        files:
        - path: environments/${PATH}/cmk-common/**.yaml
    template:
      metadata:
        name: '{{releaseName}}'
      spec:
        project: default
        source:
          repoURL: https://github.com/credmark-devops/gitops.git
          targetRevision: HEAD
          path: helm/cmk-common
          helm:
            version: v3
            passCredentials: false
            valueFiles: 
             - '../../environments/${PATH}/cmk-common/{{releaseName}}.yaml'
            releaseName: '{{releaseName}}'
        destination:
          server: https://kubernetes.default.svc
          namespace: default